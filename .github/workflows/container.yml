name: Container
on: push

jobs: 
  node-docker:
    runs-on: ubuntu-latest
    container: 
      image: node:16.15-alpine3.14
    steps:
      - name: Log Node Version
        run: |
          node -v 
          cat /etc/os-release

